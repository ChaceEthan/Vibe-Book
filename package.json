{
  "name": "vibebook",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "security:scan": "deepsec scan",
    "security:report": "deepsec export --format json --out deepsec-report.json",
    "security:fix": "deepsec scan"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "deepsec": "^1.1.13"
  }
}
